{
  "name": "silo-extensions",
  "private": true,
  "scripts": {
    "build": "cd docs-panel && npm run build",
    "build:all": "for dir in docs-panel; do (cd \"$dir\" && npm run build); done"
  }
}
