{
  "name": "silo-extensions",
  "private": true,
  "scripts": {
    "build": "cd web-viewer && npm run build",
    "build:all": "for dir in docs-panel web-viewer; do (cd \"$dir\" && npm run build); done"
  }
}
