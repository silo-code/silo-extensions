{
  "name": "@silo-extensions/agent-monitor",
  "displayName": "Agent Monitor",
  "version": "0.2.7",
  "description": "At-a-glance agent status: an Agents view in the Navigator groupable by status or workspace, status rows on each workspace, and terminal tab badges/icons, cleared when you view the terminal.",
  "type": "module",
  "files": [
    "dist/",
    "build.mjs"
  ],
  "silo": {
    "id": "silo.agent-monitor",
    "engine": "^0.42.0",
    "main": "dist/index.js",
    "publisher": "Silo",
    "permissions": []
  },
  "scripts": {
    "build": "node build.mjs",
    "build:watch": "node --watch build.mjs",
    "typecheck": "tsc --noEmit",
    "test": "vitest run"
  },
  "devDependencies": {
    "@phosphor-icons/react": "^2.1.10",
    "@silo-code/sdk": "^0.34.0",
    "@types/react": "^19.0.0",
    "esbuild": "^0.28.0",
    "react": "^19.0.0",
    "typescript": "^5.6.0",
    "vitest": "^4.1.7"
  }
}
